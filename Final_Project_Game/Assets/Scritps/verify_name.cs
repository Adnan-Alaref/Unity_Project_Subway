using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class verify_name : MonoBehaviour
{
    // Start is called before the first frame update
    public GameObject verifying;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
